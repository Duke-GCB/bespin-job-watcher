version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6-stretch
    steps:
      - checkout
      - run:
          name: install-npm
          command: npm install --dev
      - run: # run tests
          name: test
          command: npm test
